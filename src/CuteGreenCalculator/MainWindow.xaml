<Window x:Class="CuteGreenCalculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:CuteGreenCalculator.Controls"
        mc:Ignorable="d"
        Title="Cute Green Calculator"
        Icon="pack://application:,,,/Assets/app.ico"
        Width="496" Height="840"
        MinWidth="248" MinHeight="420"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen">

    <!--
        MainWindow only owns window-level chrome (title, sizing, resize behavior).
        All calculator visuals/behavior live in CalculatorView so the chrome can be
        swapped for a custom borderless frame later without touching the face.
    -->
    <controls:CalculatorView x:Name="Calculator" />
</Window>
